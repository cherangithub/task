
apiVersion: v1
kind: Service
metadata:
  name: flask-service
  labels:
{{ include "labels" . | nindent 4 }}
spec:
  selector:
    app: flask-app
  ports:
    - port: 80
      targetPort: 5000
  type: NodePort