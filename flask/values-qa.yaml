app:
  replicas: 1
  image: charantangisetti/flask-application:latest
  env: qa
  compute:
    requests:
      cpu: 0.5
      memory: 500Mi
    limits:
      cpu: 0.5
      memory: 500Mi

hpa:
  enabled: false
  minReplicas: 5
  maxReplicas: 10

ingress:
  host: prod.flask.local